# Fase 4 — Admin — Implementation Plan

> REQUIRED SUB-SKILL: superpowers:subagent-driven-development.

**Goal:** Selaraskan admin panel dengan Blueprint secara konservatif (font-heading, Card, EmptyState). ZERO logic change.

**Tech:** Next.js 16, Tailwind v4. Admin = client components.

**Aturan global tiap file:** baca file dulu; hanya ubah (a) `<h1>`/judul → tambah `font-heading`; (b) container `glass-card` → `Card` (`import { Card } from "@/components/ui/card";`) untuk WADAH non-interaktif — pertahankan className lain (padding, flex, dll) di prop `className` Card; (c) empty-state teks polos → `EmptyState`. JANGAN ubah handler, fetch, mutation, form, state, nav logic, route. Verifikasi `pnpm build 2>&1 | grep -iE "compiled successfully|error"` (bila ENOTEMPTY → `rm -rf .next` lalu ulang) setelah tiap task.

---

## Task 1: Shell + Dashboard

**Files:** `app/(admin)/admin/layout.tsx`, `app/(admin)/admin/page.tsx`

### layout.tsx
- [ ] Pada brand `<Link href="/admin" className="text-[#d4a72c] font-extrabold text-lg block mb-8">IKASI Admin</Link>` → tambahkan `font-heading` ke className.

### page.tsx (dashboard)
- [ ] `import { Card } from "@/components/ui/card";`
- [ ] `<h1 className="text-3xl font-extrabold tracking-tight mb-8">Dashboard</h1>` → tambah `font-heading`.
- [ ] Stat cards: ganti `<Link key={s.label} href={s.href} className="glass-card rounded-2xl p-5 hover:border-[#d4a72c]/40 transition-colors ${s.urgent ? ...}">` → `<Card key={s.label} href={s.href} className={\`p-5 ${s.urgent ? "border-orange-500/30" : ""}\`}>` (+ tutup `</Card>`). Pertahankan isi & logika urgent.
- [ ] Action cards: ganti `<Link key={a.href} href={a.href} className="glass-card rounded-2xl p-5 hover:border-[#d4a72c]/40 transition-colors">` → `<Card key={a.href} href={a.href} className="p-5">` (+ tutup `</Card>`).

- [ ] Build. Commit: `git add "app/(admin)/admin/layout.tsx" "app/(admin)/admin/page.tsx" && git commit -m "feat(admin): font-heading + Card di shell & dashboard"`

---

## Task 2: List pages (alumni, duplicates, deletions, news, events, wa-groups)

**Files:** `app/(admin)/admin/alumni/page.tsx`, `app/(admin)/admin/alumni/duplicates/page.tsx`, `app/(admin)/admin/alumni/deletions/page.tsx`, `app/(admin)/admin/news/page.tsx`, `app/(admin)/admin/events/page.tsx`, `app/(admin)/admin/wa-groups/page.tsx`

Untuk SETIAP file: baca dulu, lalu terapkan aturan global:
- [ ] Tambah `font-heading` ke `<h1>` halaman.
- [ ] `import { EmptyState } from "@/components/ui/empty-state";` bila ada empty state teks polos; ganti `<div className="...text-center...">Tidak ada ...</div>` (atau sejenis) dengan `<EmptyState title="..." description="..." />` (pakai teks yang ada sebagai title; description singkat).
- [ ] Bila ada container baris/kartu `glass-card` yang merupakan WADAH (bukan tombol), boleh ganti ke `Card` (`import { Card } from "@/components/ui/card";`, `className` pertahankan padding/flex). Bila barisnya kompleks dengan banyak handler, AMAN untuk membiarkannya `glass-card` — prioritas jangan rusak. Minimal wajib: font-heading h1 + EmptyState.
- [ ] JANGAN ubah: `toggleVerify`, fetch/pagination, checkbox filter, tombol verifikasi/hapus/restore, `useSearchParams`/`Suspense`.

Contoh untuk `alumni/page.tsx`: `<h1 className="text-2xl font-extrabold tracking-tight mb-6">Verifikasi Alumni</h1>` → tambah `font-heading`; `<div className="text-slate-500 text-center py-12">Tidak ada alumni ditemukan.</div>` → `<EmptyState title="Tidak ada alumni ditemukan" description="Coba ubah kata kunci atau filter." />`.

- [ ] Build setelah semua 6 file. Commit: `git add "app/(admin)/admin/alumni/page.tsx" "app/(admin)/admin/alumni/duplicates/page.tsx" "app/(admin)/admin/alumni/deletions/page.tsx" "app/(admin)/admin/news/page.tsx" "app/(admin)/admin/events/page.tsx" "app/(admin)/admin/wa-groups/page.tsx" && git commit -m "feat(admin): font-heading + EmptyState + Card di list pages"`

---

## Task 3: Editor pages (judul saja)

**Files:** `app/(admin)/admin/news/[id]/page.tsx`, `app/(admin)/admin/news/[id]/news-editor-client.tsx`, `app/(admin)/admin/wa-groups/[id]/page.tsx`, `app/(admin)/admin/wa-groups/[id]/wa-group-editor-client.tsx`

Untuk SETIAP file: baca dulu. **Hanya** tambahkan `font-heading` ke `<h1>`/judul halaman editor (bila ada). JANGAN sentuh form, input, handler, save logic. Bila file tidak punya h1 jelas, lewati tanpa perubahan.

- [ ] Build. Commit (bila ada perubahan): `git add "app/(admin)/admin/news/[id]/page.tsx" "app/(admin)/admin/news/[id]/news-editor-client.tsx" "app/(admin)/admin/wa-groups/[id]/page.tsx" "app/(admin)/admin/wa-groups/[id]/wa-group-editor-client.tsx" && git commit -m "feat(admin): font-heading pada judul editor"`

---

## Task 4: QA + build
- [ ] `pnpm test:run` → 48 PASS.
- [ ] `rm -rf .next && pnpm build` → sukses.
- [ ] Lapor jumlah file berubah + commit SHA. (Visual QA admin perlu login admin — bila tak bisa, cukup pastikan build & dashboard render tanpa error di kode.)

## Self-Review
- Coverage: shell+dashboard (T1), 6 list (T2), 4 editor (T3), QA (T4).
- ZERO logic change ditegaskan di tiap task. Build = gate utama keamanan.
